
export enum Pages {
  HOME = '/home',
  MISSIONS = '/missions',
  FRIENDS = '/friends',
  ROOT = '/',
  FORBIDDEN = '/forbidden',
}

export enum MissionFilterType {
  COMPLETED = 'completed',
  REMAINING = 'remaining',
}

export enum BloodStatus {
  CHARGING = 'CHARGING',
  DRAINING = 'DRAINING',
  SPOILING = 'SPOILING',
}