import { Typography } from '@mui/material';
import { type FC } from 'react';


const Missions: FC = () => {
  return (
    <Typography variant="caption-12-medium" color="text.primary" >Missions</Typography>
  );
};

export default Missions;